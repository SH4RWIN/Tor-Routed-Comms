# For the server, leave SERVER_IP as localhost.
# For the client, replace "your_onion_address.onion" with the server's actual .onion address.
SERVER_IP=your_onion_address.onion

# The port must match the port configured for the Tor hidden service on the server.
PORT=4444
